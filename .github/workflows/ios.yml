name: Build and test workflow

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  build:
    name: Build and Test default scheme using any available iPhone simulator
    runs-on: macos-12
    # https://github.com/actions/virtual-environments/blob/main/images/macos/macos-12-Readme.md
    #strategy:
    #  matrix:
    #    run-config:
    #      - { scheme: 'SPISearch', destination: 'platform=macOS' }
    #      - { scheme: 'SPISearch', destination: 'platform=iOS Simulator,OS=15.2,name=iPhone 8' }

    steps:
      - name: Checkout
        uses: actions/checkout@v3

      - name: Select Xcode 13.4.1
        run: xcode-select -s /Applications/Xcode_13.4.1.app
      
      #- name: Show what's in Applications
      #  run: ls -al /Applications

      - name: Show Build Version
        run: xcodebuild -version

      #- name: Show Build Settings
      #  run: xcodebuild -showBuildSettings

      - name: Show Build SDK
        run: xcodebuild -showsdks
      # iOS 15.2, macOS 12.1 on macOS-latest (11)

      - name: buildTrial
        run: xcodebuild clean test -scheme 'SPISearch' -destination 'platform=iOS Simulator,OS=15.2,name=iPhone 8' -showBuildTimingSummary
